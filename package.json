{
  "name": "task-progress",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Link-first read-only task progress reports for public pages and local projects.",
  "scripts": {
    "test": "node --test",
    "checklist:ui:build": "vite build --config experiments/editor-svelte-spike/vite.checklist-ui.config.js",
    "checklist:browser:build": "vite build --config experiments/editor-svelte-spike/vite.checklist-browser.config.js",
    "viewer:ui:build": "vite build --config experiments/editor-svelte-spike/vite.viewer-ui.config.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "svelte": "^5.56.8",
    "vite": "^8.2.0"
  }
}
