# 第三方 Checklist 單項寫入 Checklist

Current round: `Documentation/ThirdPartyChecklistWritePlan.md#驗證`.

本 round 僅涵蓋階段 2／3 的 HTTP 授權與端點探索。階段 1 的已完成 round 保存於 commit 46893e2；本 round 完成前不替換。依使用者 2026-09-08 指示，完整驗收與實機測試延後，未執行的 checks 保持未勾選。

- [ ] **1. 第三方 HTTP 授權只開放 Checklist**
  Outcome: 無 Origin 的 Checklist 客戶端可用獨立 bearer token 呼叫既有 bridge，其他編輯路由的授權邊界不變。
  Checks:
    - [ ] **授權矩陣與瀏覽器相容**
      - Action: 執行 Python host 的正確／錯誤／缺少 token、含 Origin、重複 Authorization 與既有瀏覽器路徑測試。
      - Expect: 只有核定路徑獲准；token 不可用於 report 編輯或 LocalWebService 控制 API。

- [ ] **2. 端點探索遵守服務生命週期**
  Outcome: 服務啟動發布當前 port 的私有端點檔，正常停止刪除自己的檔案，重啟更換 token，不改動其他 port。
  Checks:
    - [ ] **生命週期與判活**
      - Action: 驗證啟動／停止／重啟／沿用服務、同 port 陳舊檔替換、其他 port 檔案保留及錯誤服務占用 port 的情境。
      - Expect: 端點只含公開連線欄位與 client token；沿用服務不輪換 token，停止只清本次 token 的檔案；呼叫端需以 health 的 service 欄位確認身份。

- [ ] **3. Launcher 傳遞私人端點目錄**
  Outcome: Launcher 依應用程式 home 傳入 endpoints 目錄，以目前使用者專用 ACL 保護，host 拒絕靜態公開該位置。
  Checks:
    - [x] **必要建置與語法檢查**
      - Action: 使用已安裝的 .NET 9.0.315 MSBuild 建置 CLI（不 restore），並解析 taskprogress_host.py 的 Python 語法。
      - Expect: C# 建置成功且 Python 語法合法。
      - Observed: 預設 .NET 10.0.400 的 no-restore 建置回 NETSDK1127，未解析到專案所需的 targeting packs。
      - Resolved: 改用已安裝的 .NET 9.0.315 MSBuild 後建置成功，未下載依賴；Python AST 解析通過。
    - [ ] **權限與曝露邊界**
      - Action: 在隔離目錄驗證 ACL、TASK_PROGRESS_HOME／port 對應、啟動參數、Viewer root／mount／exact-file 的端點曝露拒絕。
      - Expect: 其他一般使用者不能讀取 token；token 不在 argv／health／公開 routes，錯誤目錄設定拒絕啟動。

- [ ] **4. 第三方與人工後路共用同一份結果**
  Depends on: 1, 2, 3.
  Outcome: 第三方可探索端點並寫入 manual 結果，使用者仍可手動修改；服務不可用不改動來源。
  Checks:
    - [ ] **完整往返與人工後路** `[manual]`
      - Action: 在實際消費端發出 reset／set，再於 Browser Checklist 修改同一格；停止服務後再次觸發工具操作。
      - Expect: 寫入結果與頁面一致，人工可改回 pending；服務停止時工具保持可用，Checklist 不被改動。
      - Reason: 需要使用者的消費端互動工具與實際 Browser 操作判斷。
